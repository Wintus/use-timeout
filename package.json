{
  "name": "use-timeout",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "react": "^16.12.0"
  },
  "devDependencies": {
    "@types/react": "^16.9.16",
    "typescript": "^3.7.3"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -r dist",
    "clean-build": "yarn clean && yarn build"
  }
}
